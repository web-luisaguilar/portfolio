import { LoadingIcon } from '@/components/Icons'

const Loading = () => {
  return (
    <main className="flex flex-col items-center justify-center min-h-[80vh]">
      <LoadingIcon size="70px" />
    </main>
  )
}

export default Loading
